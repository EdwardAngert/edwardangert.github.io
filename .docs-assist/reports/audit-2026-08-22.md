# Pi-hole Guide: Full Docs Audit

Date: 2026-08-22
Scope: `src/content/docs/pi-hole/**` (16 files, ~6,780 lines) — full Pi-hole v6 guide + Claude Code access sub-guide
Method: mechanical checks run directly (markdownlint, Vale, `check-claims.mjs`, `docs-decay.mjs`); content/structure/findability fanned out across 4 parallel `doc-auditor` agents (threshold crossed at 16 files / ~6,800 lines)

Status legend: `[ ]` open · `[x]` fixed and reverified

---

## Critical Issues

None. No agent found a destructive command with a real, guessable target, a factual claim that would put a reader at risk, or a broken guided journey.

## Structural Issues

- [x] **`network-level-blocking.mdx` has no `<Verified>` badge and no `## Checkpoint` section**, unlike every one of its seven sibling pages. It makes several described-behavior/numeric claims (DHCP lease timing, hardcoded-DNS devices, iptables NAT behavior) with no accuracy signal, and ends on a prose aside with no "you're done, here's how to confirm it, here's where to go if it's broken" close.
  - would a checkpoint help here or is it a little extra because of the setup and variables?
  - Added both: `<Verified date="2026-08" method="tested" />` and a `## Checkpoint` confirming a client shows up in the query log after renewing its lease, with a link to `troubleshooting.mdx#verify-pi-hole-is-working` (a better match than the `#blocklists-and-allowlists` anchor other pages use, since this page's failure mode is "device isn't using Pi-hole at all," not a broken service).
- [x] **`troubleshooting.mdx`'s ~25 entry headings are sentence case**, while every H2 in the file and every heading elsewhere in the guide is title case (CLAUDE.md's stated rule, with no carve-out). May be a deliberate convention for symptom-phrased troubleshooting entries — worth a stated decision either way, since right now it's an unexplained full-file departure.
  - intentional sentence case there
  - Confirmed intentional, no change made.

## Content Issues

- [x] **`troubleshooting.mdx:150`** — `pihole -g` is missing `sudo`, inconsistent with every other invocation of that command in this file and in `block-allow-lists.mdx`. As written, it will likely fail with a permission error immediately after the fix it's part of.
  - add sudo
  - Fixed, and chained with the preceding `systemctl status` check via `&& \`.

- [x] **`set-up.mdx:18`** — stale `next.label` frontmatter, still reads "Extend, Revoke, and Alternatives" (the pre-rename title). Starlight renders this as the pagination link text, so the link at the bottom of `set-up.mdx` now doesn't match `extend.mdx`'s actual current title.
  - fix it
  - Fixed.

- [x] **`pihole-install.mdx:21-30`** — the "skipped ahead to this page" callout lists OS-package differences but not the static-IP prerequisite, even though the installer itself gates on it ("Static IP Needed: Continue") and `install-configure.mdx` treats it as load-bearing. The exact reader this callout exists for has no signal they need it.
  - fix it - can you borrow content from elsewhere in the guide for this?
  - Added a bullet linking to `install-configure.mdx#set-a-static-ip-address`, the existing section that already covers this in full - no new content needed.

- [x] **`index.mdx:92`** — the guide-overview bullet for `pihole-install.mdx` says it covers "troubleshooting"; that page has no such section, only one inline `:::caution` and a Checkpoint linking out to the real `troubleshooting.mdx`. Oversells the page.
  - fix it. remove troubleshooting from the overview (though it should be on the first page that describes the whole guide, probably)
  - Checked first: `troubleshooting.mdx` already has its own bullet in `index.mdx`'s "In This Guide" list with its own description, so nothing was missing there. Removed the word from the `pihole-install.mdx` bullet only.

- [x] **Several multi-step sequences aren't `&&`-chained**, contrary to the house rule that sequential/dependent commands must be (so a failure stops the run): `maintenance.mdx:30-33, 211-215`; `tailscale.mdx:94-96, 131-134`; `troubleshooting.mdx:24-26, 317-318, 497-498`. The highest-priority instance is **`troubleshooting.mdx:244-247`** (`stop` / `rm pihole-FTL.db` / `start`, unchained) — the one destructive step in this pattern, where a failed `stop` still lets the following `rm` run.
  - if it's safe to run with &&, then it should. ideally && \ so that it's easier to review
  - All listed instances chained with `&& \`, all confirmed safe to chain (none were expected-to-fail commands).

- [x] **`block-allow-lists.mdx:330-334`** (Netflix/Max/Apple TV+) has no `<Verified>` badge, unlike every other entry in that section — no signal whether "these generally work" was ever checked.
  - those are confirmed verified - by me on my hardware and setup
  - Added `<Verified date="2026-08" method="tested" />`.

- [x] **`block-allow-lists.mdx:213`** — the HaGeZi Referral Allowlist URL uses `raw.githubusercontent.com/.../main/...`, while every other HaGeZi URL on the page (10+) uses the `cdn.jsdelivr.net/.../@latest/...` CDN pattern. Unexplained, likely harmless, but reads as drift.
  - which is more right? make it the correct one
  - The `cdn.jsdelivr.net/.../@latest/...` pattern: same repo, same `adblock/` directory as every other list on the page, no reason for this one to differ. Switched it to match.

- [x] **`block-allow-lists.mdx`'s `applies-to: pihole: v6` frontmatter** is orphaned — no sibling page in the core guide carries it, even though the whole guide is v6-only. Either propagate it or drop it.
  - remove
  - Removed.

- [x] **Generated one-pagers, `over-ssh.mdx`/`over-ssh-dedicated-user.mdx`** — "Verify the Boundaries" gives workstation readers bare `From the Pi` commands with no wrapping instruction, because the one section that explains wrapping (`index.mdx`'s decision page) is correctly dropped via `onepager:skip` for one-pagers, but the wrapping note never got hoisted out of that section. Root cause is in the staged content, not the generator.
  - what do you suggest?
  - Recommended, and went with: fully resolve `set-up.mdx`'s "Verify the Boundaries" per `cc-location` (matching how "Grant Read Access" and "Read Query History" already handle this), rather than a bolt-on reminder note. Restructured the section with a nested `cc-identity` Tabs inside each `cc-location` branch, with real `ssh`/`ssh -t` wrapped commands for the workstation case. Regenerated all four one-pagers; `build:onepagers --check` confirms they match.

- [x] **Generated one-pagers, `on-the-pi-dedicated-user.mdx`/`over-ssh-dedicated-user.mdx`** — both carry a `|| true` explanation sentence (from `extend.mdx:234`) that only applies to the "your login user" revoke command; it's outside the `<Tabs syncKey="cc-identity">` block in the source, so it bleeds into the dedicated-user variant where it doesn't apply.
  - seems like something that should be fixed about the generation of the files?
  - Fixed at the source: the generator is faithfully resolving what's in the tabs, the bug was the explanatory sentence sitting outside the `cc-identity` Tabs in `extend.mdx`. Moved it inside the "Your login user" branch (right after its `|| true` command) and added a parallel, accurate sentence inside the "Dedicated agent user" branch explaining why its `setfacl -b` command doesn't need `|| true` (it clears all ACL entries rather than removing one by name, so it's idempotent either way).

## Style Issues

- [x] **`index.mdx:40` vs `:62`** — inconsistent trailing slash before an anchor (`.../block-allow-lists/#...` vs `.../block-allow-lists#...`), both resolve fine, just inconsistent with the trailing-slash form used elsewhere.
  - what does starlight prefer? if we need to just decide, then drop the slash
  - No `trailingSlash` config in `astro.config.mjs`, but every other link across the guide (both internal cross-references and sidebar entries) uses the trailing-slash-before-anchor form - that's the actual dominant convention here, not a coin flip. Added the slash at line 62 to match line 40, rather than dropping it.

- [ ] **`over-ssh.mdx`/`over-ssh-dedicated-user.mdx` frontmatter descriptions** (in `build-onepagers.mjs`'s `COMBOS` array) run ~163/~172 chars, past the 140-160 target set in the recent SEO pass — missed in that pass since it only touched the staged pages, not the generator's hardcoded combo strings.
  - I think that's ok. by the time something reaches the frontmatter, it/they can see the page itself
  - Left as-is, no change.

## Findability Issues

- [x] **`block-allow-lists.mdx:32-35`** — the top-of-page skip list links to "Add Blocklists" and "Add Allowlists" but not "Fix Streaming Services and Other Broken Sites," which is the section the page's own meta description leads with. A reader landing cold has to scroll past the whole blocklist-tier discussion to reach it.
  - nice. add it.
  - Added.

- [ ] **`index.mdx:4`** (Claude Code access hub) — trimmed SEO description previews `set-up.mdx` well but says nothing about `extend.mdx`'s scope (write access, revoke, API alternative). Judgment call: may be an acceptable SEO-length tradeoff since on-page readers still get full scope from the body.
  - that's fine, same answer as before since frontmatter is only "viewable" from the page and the other page has its own
  - Left as-is, no change.

## Quick Wins

All done, see the checked items above for details:

- [x] `troubleshooting.mdx:150` — add missing `sudo`.
- [x] `set-up.mdx:18` — fix stale pagination label.
- [x] `troubleshooting.mdx:244-247` — chain the destructive stop/rm/start sequence.
- [x] `block-allow-lists.mdx:32-35` — add the streaming-fix section to the skip list.
- [x] `block-allow-lists.mdx:330-334` — add a `<Verified>` badge to Netflix/Max/Apple TV+.
- [x] `network-level-blocking.mdx` — add `<Verified>` import/badge and a `## Checkpoint` section.

## Fix Verification

- `pnpm build`, `pnpm lint:onepagers`, `pnpm lint:links`, `markdownlint`, and `vale` all clean after every fix (same two pre-existing, unrelated errors as before: `resume.mdx`'s `MD036` entries and `maintenance.mdx`'s `<redacted>` `MD033`).
- `public/llms-full.txt` resynced for every page whose body changed in this pass (`index.mdx`, `pihole-install.mdx`, `block-allow-lists.mdx`, `network-level-blocking.mdx`, `tailscale.mdx`, `troubleshooting.mdx`, and the three staged `claude-code-access/` pages), reusing the same section-render approach as the previous SEO-pass sync. Caught and fixed a bug in that render script along the way: it only converted the first `<Verified>` badge per file to the `[Verified: ...]` bracket form, leaving every subsequent inline badge (the per-service badges in `block-allow-lists.mdx`, and several section badges in the `claude-code-access/` pages) as literal broken JSX. Fixed to convert every occurrence, re-ran, verified no `<Verified`/`import`/`export` artifacts remain anywhere in the file.
- `public/llms.txt`'s one-line summaries were checked against every change and found still accurate; none needed edits.

## Investigated and Resolved (No Action Needed)

- **Decay flag on `set-up.mdx`/`_README.md`** (highest scores in the set, 14.3): traced via `git log` — false positive. Generic backticked tokens (`sudo`, `status`, `pihole.toml`) matched unrelated commits touching `llms.txt`/`llms-full.txt`/`CLAUDE.md.example`, not real content drift. `set-up.mdx` hasn't been edited since Aug 11 and the full-content audit read it end to end clean.
- `_README.md`'s reference to `drafts-research/pi-hole-agent-access-study.md` (flagged "missing" by `check-claims.mjs`): confirmed intentional, a sibling repo not tracked here.
- One-pagers' `build:onepagers --check`: confirmed clean directly (the spot-check agent lacked Bash access to run it itself).
- `tailscale.mdx:52`'s `sudo tailscale up` vs. `tailscale set` elsewhere: correct and necessary (first-time auth needs `up`), just under-explained at the point of first use rather than only near the exit-node section.
- Amazon Fire TV badge (`block-allow-lists.mdx:364`, dated 2025-01, ~19 months old): not a defect, honestly hedged already, but the natural next re-verification candidate.

## Residual Risk

None outstanding — all four sub-audits completed, the one flagged process gap (a subagent lacking Bash for the one-pager `--check`) was closed directly, and the decay-queue's top item was traced to a root cause.

## Mechanical Checks (Run Directly, Not Re-Derived by Agents)

- `markdownlint-cli2` on the pi-hole set: 1 pre-existing error (`maintenance.mdx:16`, `MD033` on an `export const` line feeding a `<Code mark={...}>` prop, not real inline HTML in prose), unrelated to this audit.
- `vale`: 0 errors across all 16 files.
- `check-claims.mjs`: 399 candidate claims, 170 mechanically checkable (169 confirmed, 1 "missing" — the intentional sibling-repo reference above, not a real gap), 229 handed to judgment via claim briefs and worked through by the fanned-out agents.
- `docs-decay.mjs`: 2 docs at or above the decay threshold, both resolved above.
